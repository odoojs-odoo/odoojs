const ModelFields = {
  state: {
    selection: [
      ['draft', 'Draft'],
      ['confirmed', 'Confirmed'],
      ['done', 'Done'],
      ['cancel', 'Cancelled']
    ]
  }
}

const AddonsFields = {
  'fp.bill.ocr': ModelFields
}

export default AddonsFields
